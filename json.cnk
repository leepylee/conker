
struct Storage
  str name
  num type
  num boxNo
end

func main()
  Storage storage
  if exists('storage.dat')
    loaddata storage, 'storage.dat'
  else
    StorageItem si = { name = 'Camp bed', type = 1, boxNo = 42 }  
    savedata storage, 'storage.dat'
  end
  print storage
end
